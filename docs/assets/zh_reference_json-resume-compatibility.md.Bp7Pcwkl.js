import{_ as s,o,c as t,a2 as a}from"./chunks/framework.CNDaxUod.js";const m=JSON.parse('{"title":"JSON Resume 兼容性评估","description":"","frontmatter":{},"headers":[],"relativePath":"zh/reference/json-resume-compatibility.md","filePath":"zh/reference/json-resume-compatibility.md","lastUpdated":1788607300000}'),c={name:"zh/reference/json-resume-compatibility.md"};function d(i,e,n,r,l,p){return o(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="json-resume-兼容性评估" tabindex="-1">JSON Resume 兼容性评估 <a class="header-anchor" href="#json-resume-兼容性评估" aria-label="Permalink to &quot;JSON Resume 兼容性评估&quot;">​</a></h1><blockquote><p>评估日期：2026-09-02 结论：当前 web-resume YAML <strong>不与 JSON Resume 直接兼容</strong>，但可以通过“JSON Resume 标准数据 + <code>x-web-resume</code> 展示扩展 + 双向适配器”渐进兼容，无需立即破坏现有简历。</p></blockquote><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>本文保留的是实施前的兼容性评估和决策依据。v0.4 已完成格式识别、校验、渲染适配、显式转换和拖拽顺序持久化；当前使用方式与未完成项以<a href="./../migrations/v0.4-json-resume">迁移指南</a>为准。</p></div><h2 id="评估基准" tabindex="-1">评估基准 <a class="header-anchor" href="#评估基准" aria-label="Permalink to &quot;评估基准&quot;">​</a></h2><p>JSON Resume 在 2026 年将 schema 迁入 <code>jsonresume/jsonresume.org</code> monorepo。官方说明 <code>packages/schema/schema.json</code> 和 <code>sample.resume.json</code> 代表当前稳定规范，包仍以 <code>@jsonresume/schema</code> 发布（<a href="https://github.com/jsonresume/jsonresume.org/blob/54eba4131801b6b32aa947ce8a4fbef526800a08/packages/schema/README.md" target="_blank" rel="noreferrer">Schema README</a>）。本次以该仓库当时 <code>master</code> 的 commit <code>54eba4131801b6b32aa947ce8a4fbef526800a08</code> 为固定基准：</p><ul><li>发布包版本是 <strong>1.3.1</strong>（<a href="https://github.com/jsonresume/jsonresume.org/blob/54eba4131801b6b32aa947ce8a4fbef526800a08/packages/schema/package.json" target="_blank" rel="noreferrer"><code>package.json</code></a>）。</li><li>canonical schema 是 JSON Schema draft-07（<a href="https://github.com/jsonresume/jsonresume.org/blob/54eba4131801b6b32aa947ce8a4fbef526800a08/packages/schema/schema.json" target="_blank" rel="noreferrer"><code>schema.json</code></a>）。</li><li>官网展示页仍标注 <code>version 1.0.0</code>（<a href="https://jsonresume.org/schema" target="_blank" rel="noreferrer">Schema 展示页</a>）。因此实现和验证应以发布包/canonical schema 为准，不应仅依赖展示页的版本文字。</li></ul><p>canonical schema 的重要行为：</p><ul><li>根对象及所有嵌套对象均允许 <code>additionalProperties: true</code>；扩展字段可通过 schema 验证，但第三方主题不一定识别或保留它们。</li><li>schema 中没有 <code>required</code>；所有 section 和字段都可省略。已声明字段不接受 <code>null</code>。</li><li>日期只接受 <code>YYYY</code>、<code>YYYY-MM</code> 或 <code>YYYY-MM-DD</code>。该正则只约束形状，不是严格的真实日历校验。</li><li>官方尚未采纳专用 <code>custom</code> 命名空间；相关提案仍为 open issue（<a href="https://github.com/jsonresume/jsonresume.org/issues/316" target="_blank" rel="noreferrer"><code>custom</code> field proposal</a>）。</li><li>schema 遵循 SemVer，破坏性公共 API 变更只应进入 major 版本（<a href="https://github.com/jsonresume/jsonresume.org/blob/54eba4131801b6b32aa947ce8a4fbef526800a08/packages/schema/README.md#versioning" target="_blank" rel="noreferrer">Versioning</a>）。</li></ul><h2 id="当前-web-resume-模型" tabindex="-1">当前 web-resume 模型 <a class="header-anchor" href="#当前-web-resume-模型" aria-label="Permalink to &quot;当前 web-resume 模型&quot;">​</a></h2><p>当前数据契约由 <a href="https://github.com/YunYouJun/web-resume/blob/main/apps/web/src/types/resume.ts" target="_blank" rel="noreferrer"><code>apps/web/src/types/resume.ts</code></a> 和 <a href="https://github.com/YunYouJun/web-resume/blob/main/apps/web/src/types/base.ts" target="_blank" rel="noreferrer"><code>apps/web/src/types/base.ts</code></a> 定义，并通过 <a href="https://github.com/YunYouJun/web-resume/blob/main/apps/web/scripts/schema.ts" target="_blank" rel="noreferrer"><code>apps/web/scripts/schema.ts</code></a> 生成项目自有 schema。主要结构是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>basics:      object</span></span>
<span class="line"><span>contact:     object&lt;string, { href, icon, label }&gt;</span></span>
<span class="line"><span>education:   { title, icon, histories[] }</span></span>
<span class="line"><span>project:     { title, icon, sets[] }</span></span>
<span class="line"><span>skill:       { title, icon, sets[] }</span></span>
<span class="line"><span>certificate: { title, icon, histories[] }</span></span>
<span class="line"><span>other:       { title, icon, info[] }</span></span>
<span class="line"><span>footer:      { link }</span></span></code></pre></div><p>渲染器 <a href="https://github.com/YunYouJun/web-resume/blob/main/apps/web/src/components/resume/All.vue" target="_blank" rel="noreferrer"><code>apps/web/src/components/resume/All.vue</code></a> 依赖 YAML 对象的 key 顺序决定 section 顺序，并仅识别 <code>education</code> / <code>project</code> / <code>certificate</code> / <code>skill</code> / <code>work</code> / <code>other</code>。本次工作已将原本只存在于组件和基础类型中的 <code>work</code> 补入 <code>ResumeInfo</code> 与生成的项目 schema，消除了这项内部不一致。</p><p>项目自有 schema 与官方规范的校验哲学也相反：它将 <code>basics</code> / <code>contact</code>、联系方式的 <code>href</code> / <code>icon</code> / <code>label</code> 以及多个展示字段设为必填；JSON Resume 则允许不完整简历。</p><h2 id="字段映射" tabindex="-1">字段映射 <a class="header-anchor" href="#字段映射" aria-label="Permalink to &quot;字段映射&quot;">​</a></h2><p>JSON Resume 的完整标准 section 为 <code>$schema</code>、<code>basics</code>、<code>work</code>、<code>volunteer</code>、<code>education</code>、<code>awards</code>、<code>certificates</code>、<code>publications</code>、<code>skills</code>、<code>languages</code>、<code>interests</code>、<code>references</code>、<code>projects</code> 和 <code>meta</code>（<a href="https://github.com/jsonresume/jsonresume.org/blob/54eba4131801b6b32aa947ce8a4fbef526800a08/packages/schema/schema.json" target="_blank" rel="noreferrer">canonical schema</a>）。</p><table tabindex="0"><thead><tr><th>web-resume</th><th>JSON Resume</th><th>转换与损失</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>basics.name</code></td><td>项目根级 <code>name</code> 是别名，导出时应合并到 <code>basics.name</code>。</td></tr><tr><td><code>basics.name</code></td><td><code>basics.name</code></td><td>直接映射。</td></tr><tr><td><code>basics.label</code></td><td><code>basics.label</code></td><td>直接映射。</td></tr><tr><td><code>basics.avatar</code></td><td><code>basics.image</code></td><td>改名；官方 <code>image</code> 只约束为 string。</td></tr><tr><td><code>basics.bio</code></td><td><code>basics.summary</code></td><td>改名。</td></tr><tr><td><code>basics.location: string</code></td><td><code>basics.location: object</code></td><td>不能无损自动拆分为 <code>address/postalCode/city/countryCode/region</code>；需要用户确认或作为 <code>city</code> 的宽松导入。</td></tr><tr><td><code>basics.sex/birth/age</code></td><td>无标准字段</td><td>保存到 <code>x-web-resume.basics</code>，或在“纯标准导出”中省略。</td></tr><tr><td><code>contact.email</code></td><td><code>basics.email</code></td><td>从 <code>label</code> 取值；<code>href</code> 的 <code>mailto:</code> 可用于交叉校验，<code>icon</code> 是展示扩展。</td></tr><tr><td><code>contact.phone</code></td><td><code>basics.phone</code></td><td>从 <code>label</code> 取值；<code>tel:</code> href 和 icon 作为展示扩展。</td></tr><tr><td><code>contact.blog</code></td><td><code>basics.url</code></td><td>优先取 <code>href</code>。</td></tr><tr><td>其他 <code>contact.*</code></td><td><code>basics.profiles[]</code></td><td>key → <code>network</code>，<code>label</code> → <code>username</code>，<code>href</code> → <code>url</code>；任意 contact 不一定都是社交资料，需允许扩展回退。</td></tr><tr><td><code>education.histories[]</code></td><td><code>education[]</code></td><td>wrapper 需展平；<code>school → institution</code>、<code>grade → score</code>、<code>start/end → startDate/endDate</code>，<code>area/studyType</code> 直接映射。</td></tr><tr><td><code>education.{title,icon}</code></td><td>无标准字段</td><td>属于 section 展示配置，保存到 <code>x-web-resume.sections.education</code>。</td></tr><tr><td><code>EducationHistory.city/logo/icon</code></td><td>无标准字段</td><td>保存到对应 item 扩展；JSON Resume education 没有 location/logo。</td></tr><tr><td><code>work.sets[]</code></td><td><code>work[]</code></td><td>wrapper 需展平；<code>name</code> 直接映射，<code>type → position</code>、<code>start/end → startDate/endDate</code>、<code>summary/highlights/url</code> 直接映射。</td></tr><tr><td><code>project.sets[]</code></td><td><code>projects[]</code> <strong>或</strong> <code>work[]</code></td><td>当前样例将雇主经历和开源项目混在 <code>project</code>，必须先按语义拆分，不能只根据字段名批量迁移。</td></tr><tr><td><code>ProjectSet.summary</code></td><td><code>work.summary</code> / <code>projects.description</code></td><td>根据上一行的 section 决定。</td></tr><tr><td><code>ProjectSet.type</code></td><td><code>work.position</code> / <code>projects.type</code></td><td><code>projects.roles[]</code> 更适合多角色情况。</td></tr><tr><td><code>ProjectSet.keywords[]</code></td><td><code>projects.keywords[]</code></td><td>当前可为 <code>{ name, icon, logo }</code> 或 string；标准只接受 string[]，图标需作为扩展保存。</td></tr><tr><td><code>ProjectSet.repo/logo/open/badges</code></td><td>无直接标准字段</td><td><code>repo</code> 可在没有 <code>url</code> 时降级为 <code>url</code>；其余保存到 item 扩展。</td></tr><tr><td><code>skill.sets[]</code></td><td><code>skills[]</code></td><td>wrapper 需展平；<code>title → name</code>，keyword object 只导出 <code>name</code> 得到 string[]。当前没有对应 <code>level</code>。</td></tr><tr><td><code>certificate.histories[]</code></td><td><code>awards[]</code> / <code>certificates[]</code> / <code>publications[]</code></td><td>当前“奖项表彰”样例混有获奖、认证和论文；需按语义拆分。<code>name/place/time</code> 可分别映射为 title/awarder/date 或 name/issuer/date。</td></tr><tr><td><code>other.info[]</code></td><td>无等价 section</td><td>不应无条件映射到 <code>interests</code>；可保存在 <code>x-web-resume.other</code>。</td></tr><tr><td><code>footer.link</code></td><td><code>meta.canonical</code> 或扩展</td><td>只有当 link 确实指向该简历最新版本时才能映射为 <code>meta.canonical</code>。</td></tr></tbody></table><p>当前完全缺少对 <code>volunteer</code>、<code>languages</code>、<code>interests</code>、<code>references</code> 和 <code>meta</code> 的渲染；<code>awards</code>、<code>certificates</code>、<code>publications</code>、<code>projects</code>、<code>skills</code> 也不会以官方 section 名被当前渲染器识别。</p><h2 id="兼容性判定" tabindex="-1">兼容性判定 <a class="header-anchor" href="#兼容性判定" aria-label="Permalink to &quot;兼容性判定&quot;">​</a></h2><table tabindex="0"><thead><tr><th>维度</th><th>现状</th><th>原因</th></tr></thead><tbody><tr><td>canonical schema 验证</td><td>不兼容</td><td>官方 <code>education</code> 必须是 array，项目中是 object；多个公开样例还将 <code>basics.location</code> 写为 string，官方要求 object。</td></tr><tr><td>标准字段语义</td><td>部分兼容</td><td><code>basics.name/label</code>、部分 project/work/education item 字段可映射，但 wrapper、命名和 section 语义不同。</td></tr><tr><td>第三方 JSON Resume 导入</td><td>不兼容</td><td>当前 schema 强制 <code>contact</code>，渲染器不识别标准复数 section 名。</td></tr><tr><td>web-resume 导出到第三方</td><td>不兼容</td><td>当前无标准化导出边界，并且日期常为“2019 年 9 月”、“至今”等非规范字符串。</td></tr><tr><td>扩展可行性</td><td>可行</td><td>canonical schema 允许 additional properties，可保留 web-resume 的 title/icon/logo/order 等展示信息。</td></tr></tbody></table><p>使用 canonical schema 对当前 <a href="https://github.com/YunYouJun/web-resume/blob/main/apps/web/src/assets/resume/local.resume.yml" target="_blank" rel="noreferrer"><code>local.resume.yml</code></a> 进行结构验证（忽略 URI/email format 插件）时，至少会在 <code>/education</code> 产生 <code>must be array</code>。其他如 <code>contact</code> / <code>project</code> / <code>skill</code> 能被放行只是因为 <code>additionalProperties: true</code>，不代表 JSON Resume 主题会理解这些自定义 section。</p><h2 id="推荐兼容策略" tabindex="-1">推荐兼容策略 <a class="header-anchor" href="#推荐兼容策略" aria-label="Permalink to &quot;推荐兼容策略&quot;">​</a></h2><h3 id="_1-以-canonical-model-作为新文档结构" tabindex="-1">1. 以 canonical model 作为新文档结构 <a class="header-anchor" href="#_1-以-canonical-model-作为新文档结构" aria-label="Permalink to &quot;1. 以 canonical model 作为新文档结构&quot;">​</a></h3><p>项目已采用独立的 <code>JsonResume</code> 类型和验证器，以 <code>@jsonresume/schema@~1.3.0</code> 为契约。为避免过早引入第三套领域对象，v0.4 暂不增加独立的 <code>NormalizedResume</code>：标准文档直接使用 JSON Resume 结构，legacy 文档通过读取适配器转换为现有渲染结构。渲染所需的 section title/icon、item logo 和展开状态统一进入 <code>x-web-resume</code>。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>legacy YAML ---- legacy adapter ----\\</span></span>
<span class="line"><span>                                  &gt; existing renderer</span></span>
<span class="line"><span>JSON Resume ---- render adapter --/</span></span>
<span class="line"><span></span></span>
<span class="line"><span>legacy YAML ---- explicit conversion ---- JSON Resume + x-web-resume</span></span></code></pre></div><p>格式检测、官方校验、转换 warning 与两个适配方向集中在同一深模块中，渲染组件不包含格式分支。若未来出现第三种持久化格式或多套独立渲染器，再评估引入 <code>NormalizedResume</code>。</p><h3 id="_2-将展示信息放入命名扩展" tabindex="-1">2. 将展示信息放入命名扩展 <a class="header-anchor" href="#_2-将展示信息放入命名扩展" aria-label="Permalink to &quot;2. 将展示信息放入命名扩展&quot;">​</a></h3><p>建议将项目专有数据放在单一根级 <code>x-web-resume</code> 下，而不是散落在每个标准对象中。这是本项目的命名约定，并非 JSON Resume 已定义的官方字段。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">x-web-resume</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  sectionOrder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">work</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">projects</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">education</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">skills</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">awards</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">other</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  sections</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    education</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">教育背景</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      icon</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">fa:graduation-cap</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  other</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">其他总结</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    icon</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ri:information-line</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">热衷开源，关注前沿……</span></span></code></pre></div><p><code>sectionOrder</code> 应取代当前对 <code>Object.keys(resume)</code> 顺序的隐式依赖；这也为拖拽卡片排序提供稳定、可持久化的数据契约。</p><h3 id="_3-提供两种导出模式" tabindex="-1">3. 提供两种导出模式 <a class="header-anchor" href="#_3-提供两种导出模式" aria-label="Permalink to &quot;3. 提供两种导出模式&quot;">​</a></h3><ul><li><strong>Portable</strong>：只导出官方字段，并用 canonical schema 验证；丢弃图标、Logo、自定义总结等不可移植展示信息。</li><li><strong>Lossless for web-resume</strong>：导出官方字段及 <code>x-web-resume</code>；仍可通过 canonical schema，且再次导入本项目时能还原展示。</li></ul><p>导出时必须将本地化日期规范化。对“至今”应省略 <code>endDate</code>，不应导出为空字符串或 <code>null</code>。无法可靠解析的日期应给出用户可操作的错误，而不是静默猜测。</p><h3 id="_4-保留-legacy-读取-逐步迁移" tabindex="-1">4. 保留 legacy 读取，逐步迁移 <a class="header-anchor" href="#_4-保留-legacy-读取-逐步迁移" aria-label="Permalink to &quot;4. 保留 legacy 读取，逐步迁移&quot;">​</a></h3><p>可根据 <code>contact</code> / <code>project</code> / <code>skill</code> 等 legacy root key，以及 <code>education</code> 是 object 还是 array 进行格式检测。编辑器在用户明确执行“转换为 JSON Resume”前不自动重写原 YAML，以避免丢失注释和手工排版。</p><h2 id="实施建议" tabindex="-1">实施建议 <a class="header-anchor" href="#实施建议" aria-label="Permalink to &quot;实施建议&quot;">​</a></h2><ol><li><p><strong>契约与回归基线（v0.4 已完成）</strong></p><ul><li>引入固定 minor 范围的 <code>@jsonresume/schema@~1.3.0</code>，增加 official sample 与最小 <code>{ basics: { name } }</code> fixture。</li><li>为现有 YAML 建立 legacy fixtures，确保兼容层不改变当前渲染结果。</li><li><code>ResumeInfo.work</code> 已在本次工作中补齐；以此作为 legacy 回归基线的一部分。</li></ul></li><li><p><strong>格式模块与适配器（v0.4 已完成核心路径）</strong></p><ul><li>已实现格式检测、legacy 显式转换、JSON Resume 校验与渲染适配。</li><li>为日期规范化、contact/profile 转换、keyword object 降级、award/certificate/publication 拆分写单元测试。</li><li>记录转换 warning，尤其是 location、语义混合的 <code>project</code> 和 <code>certificate</code>。</li></ul></li><li><p><strong>渲染覆盖</strong></p><ul><li>先支持当前已有组件可承载的 <code>basics/work/education/projects/skills/awards/certificates</code>。</li><li>再为 <code>volunteer/publications/languages/interests/references</code> 增加组件或通用 section renderer。</li><li>渲染顺序使用 <code>x-web-resume.sectionOrder</code> 或应用默认顺序，不再依赖 JSON/YAML key 顺序。</li></ul></li><li><p><strong>用户界面与迁移</strong></p><ul><li>在加载时显示检测到的格式与非破坏性 warning。</li><li>提供显式的“导出 JSON Resume”和“转换当前 YAML”操作，说明 Portable/Lossless 差异。</li><li>待公开样例和文档迁移后，再考虑将 canonical model 设为新建简历的默认格式。</li></ul></li></ol><h2 id="验收标准" tabindex="-1">验收标准 <a class="header-anchor" href="#验收标准" aria-label="Permalink to &quot;验收标准&quot;">​</a></h2><ul><li>官方 <code>sample.resume.json</code> 能直接加载，所有标准 section 均可渲染。</li><li>Portable 导出能通过与应用依赖版本一致的 canonical schema 验证。</li><li>Lossless 导出再导入后，section title/icon/order、item logo/open/badges 等项目展示数据不丢失。</li><li>所有现有公开 YAML 在 legacy 路径下保持可加载和可渲染。</li><li>非 ISO 日期、不可拆分 location 和语义模糊 section 会产生明确的转换 warning，不会静默丢数据。</li></ul><h2 id="决策" tabindex="-1">决策 <a class="header-anchor" href="#决策" aria-label="Permalink to &quot;决策&quot;">​</a></h2><p>项目决定采用“标准优先、单一扩展、兼容读取”的 v0.4 迁移策略：</p><ol><li>新建与转换后的简历使用 JSON Resume 形状。</li><li>标准无法表达的展示信息只进入根级 <code>x-web-resume</code>。</li><li>旧 YAML 自动识别并在内存中适配，未经用户确认不写回。</li><li>v0.4 保留 legacy 读取；未来移除必须通过稳定版本契约和迁移说明。</li></ol><p>完整升级与回退步骤见 <a href="./../../migrations/v0.4-json-resume">v0.4 JSON Resume 迁移指南</a>。</p>`,42)])])}const u=s(c,[["render",d]]);export{m as __pageData,u as default};
